from setuptools import setup, find_packages


setup(name='versioncheck',
      version='0.1',
      description='Version check for Firefox Addons',
      long_description='',
      author='',
      author_email='',
      license='',
      url='',
      include_package_data=True,
      classifiers=[],
      packages=find_packages(exclude=['tests']),
      install_requires=[])
